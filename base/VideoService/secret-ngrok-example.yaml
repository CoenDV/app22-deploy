# Example secret for ngrok configuration
# DO NOT commit this file with real credentials
# Create the actual secret using: oc create secret generic ngrok-secret --from-literal=authtoken=YOUR_TOKEN --from-literal=domain=YOUR_DOMAIN

apiVersion: v1
kind: Secret
metadata:
  name: ngrok-secret
type: Opaque
stringData:
  # Get your authtoken from https://dashboard.ngrok.com/get-started/your-authtoken
  authtoken: 2m99BZ760bZt0dd3n8SEjTLFkDW_3ZArNjeFGvUxXSAhA6UkE
  # Optional: Your ngrok static domain (requires paid plan)
  # If not provided, ngrok will generate a random URL
  domain: saved-ferret-rapid.ngrok-free.app
